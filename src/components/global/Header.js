import React from "react";

const Header = () => {
	return (
		<div style={styles.header}>
			<h3>Header</h3>
		</div>
	);
};

const styles = {
	header: {
		backgroundColor: "pink",
		height: 90,
	},
};

export default Header;
